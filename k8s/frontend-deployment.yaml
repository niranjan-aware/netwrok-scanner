apiVersion: apps/v1
kind: Deployment
metadata:
  name: scanner-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: scanner-frontend
  template:
    metadata:
      labels:
        app: scanner-frontend
    spec:
      containers:
      - name: frontend
        image: scanner-frontend:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: scanner-frontend
spec:
  selector:
    app: scanner-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: LoadBalancer
